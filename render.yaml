services:
  - type: web
    name: ral-dash-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_dash_ral_analysis_animated:app
    envVars:
      - key: PORT
        value: 10000
